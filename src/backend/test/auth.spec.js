const request = require("supertest");
const assert = require("assert");
const express = require("express");
const app = require("../index");

// request(app).post("/api/auth/login").send({name: "Aman"});
